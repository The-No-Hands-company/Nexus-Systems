{
  "name": "@nexus/design",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/generate.ts",
    "vendor": "bun run build && bun run scripts/vendor.ts",
    "test": "bun test tests/"
  }
}
