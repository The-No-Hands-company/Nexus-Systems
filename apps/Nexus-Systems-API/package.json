{
  "name": "nexus-systems-api-artifacts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "lint": "biome check src tests",
    "format": "biome check --write src tests",
    "check": "bun run typecheck && bun run lint",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.3.11",
    "typescript": "^5.8.3"
  }
}
